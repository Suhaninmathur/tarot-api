# Copy this to .env.local and set your deployed API URL
# During local dev, the Vite proxy handles /api → localhost:8080 automatically

# For production (after deploying to Render):
VITE_API_URL=https://your-tarot-api.onrender.com
